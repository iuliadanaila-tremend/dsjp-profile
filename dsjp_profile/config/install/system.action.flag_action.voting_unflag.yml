langcode: en
status: true
dependencies:
  config:
    - flag.flag.voting
  module:
    - flag
id: flag_action.voting_unflag
label: Unlike
type: node
plugin: 'flag_action:voting_unflag'
configuration:
  flag_id: voting
  flag_action: unflag
