{#
/**
 * @file
 * Theme override for the entity reference revisions field wich allowed to use paragraphs.
 *
 * @see ./templates/field/field--bare.html.twig
 */
#}
<div{{ title_attributes.addClass(title_classes) }}>{{ label }}</div>
{% for item in items %}
    <div class="ecl-u-mb-2xl">
        {{ item.content }}
    </div>
{% endfor %}
